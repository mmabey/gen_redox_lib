# ----  AUTO-GENERATED BY gen_redox_lib. DO NOT MODIFY MANUALLY!!  ---- #
from datetime import datetime, timezone

import pytest

from redox.abstract_base import GenericEventTypeAbstractModel
from redox.generic import PatientAdmin

round_trip_cases = (
    (
        PatientAdmin.Arrival,
        {
            "Meta": {"DataModel": "PatientAdmin", "EventType": "Arrival"},
            "Patient": {"Identifiers": [{"ID": "MySeCrEtId", "IDType": "SECRET"}]},
            "Visit": {"VisitDateTime": datetime.now(tz=timezone.utc).isoformat()},
        },
    ),
)


@pytest.mark.parametrize(("model", "dict_input"), round_trip_cases)
def test_dict_to_generic_round_trips(model: type[GenericEventTypeAbstractModel], dict_input: dict):
    generic_object = model(**dict_input)
    assert isinstance(generic_object, model)
    assert generic_object.model_dump() == dict_input
